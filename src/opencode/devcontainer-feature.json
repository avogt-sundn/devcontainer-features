{
  "id": "opencode",
  "version": "1.0.0",
  "name": "OpenCode",
  "description": "Installs the OpenCode CLI and copies its config to the user home directory at image build time.",
  "options": {
    "model": {
      "type": "string",
      "default": "mlx/gemma-4-26b-a4b-it-mlx",
      "description": "Default model ID written into opencode config."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/node"
  ]
}
